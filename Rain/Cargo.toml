[package]
name = "skydimo_rain_native"
version = "1.0.0"
edition = "2021"

[lib]
name = "rain"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"

[workspace]
