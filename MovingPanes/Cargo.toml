[package]
name = "skydimo_moving_panes_native"
version = "1.0.0"
edition = "2021"

[lib]
name = "moving_panes"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"

[workspace]
