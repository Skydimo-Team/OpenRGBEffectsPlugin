[package]
name = "skydimo_noise_map_native"
version = "1.0.1"
edition = "2021"

[lib]
name = "noise_map"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"

[workspace]
